---
- name: install openbooks on mini1.srv.3fr.uk
  hosts: mini1.srv.3fr.uk
  gather_facts: false
  tasks:
    - community.docker.docker_container:
        name: openbooks
        image: evanbuss/openbooks:latest
        pull: true
        hostname: openbooks
        restart_policy: always
        networks:
          - name: web
        env:
          PUID: "1000"
          PGID: "1000"
          TZ: "Europe/London"
        volumes:
          - openbooks_data:/books
        command: "--persist --name {{ openbooks_user }} --log --debug --tls=false -s irc.irchighway.net:6660"
        labels:
          caddy_0: ob.home.thunderweb.uk
          caddy_0.redir: https://ob.svc.3fr.uk{uri}
          caddy_1: ob.svc.3fr.uk
          caddy_1.import_0: accesslogs
          caddy_1.reverse_proxy: !unsafe "{{ upstreams 80 }}"
    - name: Create ob.svc.3fr.uk CNAME
      delegate_to: localhost
      ansible.builtin.uri:
        url: https://dns.srv.3fr.uk/api/zones/records/add
        method: POST
        body_format: form-urlencoded
        body:
          token: "{{ dns_token }}"
          domain: ob.svc.3fr.uk
          type: CNAME
          ttl: 3600
          cname: "{{ inventory_hostname }}"
          overwrite: true
        status_code: 200