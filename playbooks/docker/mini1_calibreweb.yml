---
- name: install calibre-web on mini1.srv.3fr.uk 
  hosts: mini1.srv.3fr.uk
  gather_facts: false
  tasks:
    - community.docker.docker_container:
        name: calibre-web
        image: crocodilestick/calibre-web-automated:latest
        pull: true
        hostname: calibre-web
        restart_policy: always
        networks:
          - name: web
        env:
          PUID: "1000"
          PGID: "1000"
          TZ: "Europe/London"
          NETWORK_SHARE_MODE: "true"
          HARDCOVER_TOKEN: "{{ hardcover_api }}"
        volumes:
          - calibre_config:/config
          - /data/shared/eBooks/Calibre:/calibre-library
          - /data:/data
        labels:
          caddy_0: calibre.home.thunderweb.uk
          caddy_0.redir: https://calibre.svc.3fr.uk{uri}
          caddy_1: calibre.svc.3fr.uk
          caddy_1.import_1: accesslogs
          caddy_1.handle: "/opds/*"
          caddy_1.handle.reverse_proxy: !unsafe "{{ upstreams 8083 }}"
          caddy_1.handle.reverse_proxy.header_up: 'Authorization "Basic: {{ calibre_hash }}"'
          caddy_1.reverse_proxy: !unsafe "{{ upstreams 8083 }}"
    - name: Create calibre.svc.3fr.uk CNAME
      delegate_to: localhost
      ansible.builtin.uri:
        url: https://dns.srv.3fr.uk/api/zones/records/add
        method: POST
        body_format: form-urlencoded
        body:
          token: "{{ dns_token }}"
          domain: calibre.svc.3fr.uk
          type: CNAME
          ttl: 300
          cname: "{{ inventory_hostname }}"
          overwrite: true
        status_code: 200