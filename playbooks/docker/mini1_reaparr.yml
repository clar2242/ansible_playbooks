---
- name: install reaparr on mini1.srv.3fr.uk
  hosts: mini1.srv.3fr.uk
  gather_facts: false
  tasks:
    - community.docker.docker_container:
        name: reaparr
        image: reaparr/reaparr:latest
        pull: true
        hostname: reaparr
        restart_policy: always
        networks:
          - name: web
        env:
          PUID: "1000"
          PGID: "1000"
          TZ: "Europe/London"
        volumes:
          - reaparr_data:/Config
          - /data/media/download:/Downloads
          - /data/media/Movies:/Movies
          - /data/media/TV:/TvShows
        labels:
          caddy_0: reaparr.home.thunderweb.uk
          caddy_0.redir: https://reaparr.svc.3fr.uk{uri}
          caddy_1: reaparr.svc.3fr.uk
          caddy_1.import_0: accesslogs
          caddy_1.reverse_proxy: !unsafe "{{ upstreams 7000 }}"
    - name: Create reaparr.svc.3fr.uk CNAME
      delegate_to: localhost
      ansible.builtin.uri:
        url: https://dns.srv.3fr.uk/api/zones/records/add
        method: POST
        body_format: form-urlencoded
        body:
          token: "{{ dns_token }}"
          domain: reaparr.svc.3fr.uk
          type: CNAME
          ttl: 3600
          cname: "{{ inventory_hostname }}"
          overwrite: true
        status_code: 200